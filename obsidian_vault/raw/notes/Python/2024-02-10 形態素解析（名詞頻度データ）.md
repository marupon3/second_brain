---
メモ: 2024-02-10
source: OneNote
作成時刻: 19:15
---

# 形態素解析（名詞頻度データ）

pythonプログラミング逆引き大全 #295改良
Analyzer.pyが必要
<<analyzer.py>>
#
テキストファイルを読み込んで名詞の頻度表を作る

<!-- 変換時の注記: OneNote由来の改行を括弧深さ/構文解析で自動復元しました。インデントはOneNote側で失われている場合があり保証されません。実行前に必ず目視確認してください（要確認）。 -->

```python
import os
import re
import analyzer  # analyzerモジュールをインポート
import tkinter as tk
from tkinter import filedialog
def make_freq(file):
    """テキストファイルを読み込んで形態素解析結果を返す Args:
        file (str): テキストファイルのパス Results:
        list: 解析結果を格納した多重リスト
    """ print('テキストを読み込んでいます') with open(file, 'r', encoding='utf_8') as f:
        text = f.read()
    text = re.sub('\n', '', text)
    word_dic = {}
    analyze_list = analyzer.analyze(text)
    for wd, part in analyze_list:
        if analyzer.keyword_check(part):
            if wd in word_dic:
                word_dic[wd] += 1
            else:
                word_dic[wd] = 1
    return word_dic
def show(word_dic):
    """頻度表を出力する Args:
        word_dic (dict): 頻度表としての辞書 """ for word in sorted(word_dic, key=word_dic.get, reverse=True):
        print(word + '(' + str(word_dic[word]) + ')')
def save_analysis_results(file_path, word_dic):
    """形態素解析結果をファイルに保存する Args:
        file_path (str): 元のテキストファイルのパス word_dic (dict): 形態素解析の結果 """ # ファイル名を生成 base, ext = os.path.splitext(file_path) new_file_path = f"{base}_形態素解析.txt" with open(new_file_path, 'w', encoding='utf_8') as f:
        for word, count in sorted(word_dic.items(), key=lambda item: item[1], reverse=True):
            f.write(f"{word}: {count}\n")
    print(f"形態素解析結果を保存しました: {new_file_path}")
if __name__ == '__main__':
    root = tk.Tk()
    root.withdraw()
    file_path = filedialog.askopenfilename( title='ファイルを選択してください', filetypes=[('テキストファイル', '*.txt'), ('すべてのファイル', '*.*')] )
    if file_path:
        freq = make_freq(file_path)
        show(freq)
        save_analysis_results(file_path, freq)
        print('プログラムを終了します。')
    else:
        print('ファイルが選択されませんでした。')
```
