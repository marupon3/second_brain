---
メモ: 2025-04-20
source: OneNote
作成時刻: 8:16
---

# git設定

pycharmのターミナルで目的の目的のフォルダに設定する
gitを初期化する
(venv) PS C:\users\marupon\PycharmProjects\pulldownlist> git init
repositoryが初期化される
pycharmの左側ウィンドウで対象ファイルを選択
右クリック > git > ファイルのコミット
右クリック > git > +追加
①左側ウィンドウで「コミット」を選択
②ファイルを選択
③コメントを入力
④「コミット」ボタンを押下
![](file2122.files/image001.png)
.gitignore を使ってフォルダをGitの管理対象から外す
1. PowerShellを開き、プロジェクトフォルダへ移動：
2. .gitignore
ファイルに対象フォルダを追加：
.gitignore を作成または編集
プロジェクトフォルダに .gitignore
という名前のファイルを作成します。すでにある場合は編集します。
対象フォルダを .gitignore に追加
.gitignore に、Gitの管理から除外したいフォルダの名前を記入します。例えば、フォルダ名が venv の場合、以下のように記述します
venv/
1. ステージングから削除：
git
rm -r --cached venv
1. 変更をコミット：
git
commit -m venv
