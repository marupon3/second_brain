---
メモ: 2024-03-24
source: OneNote
作成時刻: 7:05
---

# pythonツール集

# /app/routes.py
# 文字数カウント関数（ファイル名取得、ファイルアップロード）

<!-- 変換時の注記: OneNote由来の改行を括弧深さ/構文解析で自動復元しました。インデントはOneNote側で失われている場合があり保証されません。実行前に必ず目視確認してください（要確認）。 -->

```python
def init_routes(app):
@app.route('/') def home():
return render_template('upload.html')
@app.route('/upload', methods=['POST']) def upload_file():
#
ファイルアップロードの処理
        if 'file' not in request.files:
            flash('ファイルがありません')
           return
redirect(url_for('home'))
        file = request.files['file']
        if file.filename == '':
            flash('ファイルが選択されていません')
            return
redirect(url_for('home'))
        if file:
            filename = secure_filename(file.filename)
            filepath = os.path.join(app.config['UPLOAD_FOLDER'], filename)
            file.save(filepath)
            return
redirect(url_for('process_file', filename=filename))  # 関数名変更の影響なし
        return
redirect(url_for('home'))
@app.route('/process', methods=['POST']) def process_action():
action
= request.form.get('action') # menu.html から渡されたファイル名を取得 filename = request.form.get('filename') if action == 'character_count':
# 文字数カウントの処理を呼び出す
return
redirect(url_for('process_file', filename=filename))
# その他の処理(未設定） # エラーページまたは適切なレスポンスを返す return render_template('error.html') @app.route('/process/<filename>', methods=['GET', 'POST']) def process_file(filename): if request.method == 'POST': action = request.form.get('action') if action == 'character_count': filepath = os.path.join(app.config['UPLOAD_FOLDER'], filename) if action == 'character_count': if not filepath.endswith('.txt'): os.remove(filepath) result_message = 'このファイルは対象外です。' else: count = character_count(filepath) result_message = f'文字数は、{count}です。' os.remove(filepath) return render_template('result.html', result_message=result_message) else: # GET リクエストの場合は、処理選択フォームを表示 return render_template('menu.html', filename=filename) # run.py #起動ファイル from app import app if __name__ == "__main__": app.run(debug=True) """ Flask_Processing_Project/ ├── app/ │     ├── templates/ │     │ ├── upload.html　(処理用ファイルを選択する) │     │ ├── menu.html　(実行する処理を選択する) │     │ ├── result.html　(結果を表示する) │     │ └── download.html　(ダウンロードファイルボタンを表示する) │     ├── __init__.py │     ├── processing.py　(データ処理) │     └── routes.py　(アップロードとダウンロードの機能) ├── upload/ │     └── uploadfile　(処理前のファイルの保管) ├── download/ │     └── downloadfile　(処理後のファイルの保管) └── run.py　（アプリケーションを実行するためのファイル） """ # app/__init__.py # 初期設定 from flask import Flask app = Flask(__name__) app.config['UPLOAD_FOLDER'] = 'upload' app.config['DOWNLOAD_FOLDER'] = 'download' from app.routes import init_routes init_routes(app)  # ここで init_routes 関数を呼び出して app オブジェクトを渡す # app/processing.py def character_count(filepath): with open(filepath, 'r', encoding='utf-8') as file: content = file.read() return len(content) Flaskを使って、複数のPythonツールを利用できるポータルサイトを作るプロジェクトを実施中である。ツールは処理の異なる複数のプログラムからなる。各プログラムの具体的な操作手順と条件に従って、機能を追加してください。 今回は、PDFファイルを取り込んで、テキストを抽出、テキストファイルをダウンロードさせる機能を追加してください。(コードを追加してください） ###手順: #1:ローカル上のファイルをブラウザに取り込む。 #2:取り込んだファイルをpythonプログラムで処理する。 #3:処理後ファイルをローカル上に保存する。 ### 以下の条件を満たすコードを書いて下さい。 ###条件: 1. Step by Stepでこのプロジェクトを進める。 2. 最小限必要な機能を持つpyファイルとhtmlファイルを設定する(設定済み） 3. ファイルのアップロード機能を設定（ファイルの選択、アップロードに２段階） 4. アップロードファイルをpythonで処理 5. 処理後、結果はブラウザで表示する。または結果ファイルをブラウザからダウンロードする 6. このプロジェクトはwindows版専用として開発する。説明、解説は日本語を使う。 7. このプロジェクトは以下の構成を基準にフォルダ、ファイルを設置するが、必要に応じて変更してもよい。 8. すでに１つの機能が実装済みである。実装済みのファイルに影響を及ぼさないようにする。また、実装済みのコードに準じたコードを用いて追加機能を実装する。 9. 実行ファイル'run.py', 初期設定ファイル'__init__.py'は変更しない。追加は許可。
```
