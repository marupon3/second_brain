---
メモ: 2024-02-10
source: OneNote
作成時刻: 19:57
---

# 英単語出現頻度

pythonプログラミング逆引き大全 #280改良
#
単語の出現回数順に頻度表を並べ替える

<!-- 変換時の注記: OneNote由来の改行を括弧深さ/構文解析で自動復元しました。インデントはOneNote側で失われている場合があり保証されません。実行前に必ず目視確認してください（要確認）。 -->

```python
import os
import tkinter as tk
from tkinter import filedialog
def get_frequency(file_path):
    """単語の出現回数をカウントして頻度表を作る Args:
        file_path(str): ファイルのフルパス Returns:
        dict: テキストファイルから抽出した単語の頻度表
    """ freq = {} with open(file_path, encoding='UTF-8') as file_data:
        for line in file_data:
            tmp_lst = line.split()
            for word in tmp_lst:
                word = word.rstrip('.,:!?)"')
                word = word.lstrip('("')
                if word in freq:
                    freq[word] += 1
                else:
                    freq[word] = 1
    return freq
def save_frequency_list(file_path, freq):
    """頻度表をファイルに保存する Args:
        file_path(str): 元のファイルのフルパス freq(dict): 単語の頻度表 """ base, ext = os.path.splitext(file_path) new_file_path = f"{base}_英単語リスト.txt" with open(new_file_path, 'w', encoding='UTF-8') as file:
        for word in sorted(freq, key=freq.get, reverse=True):
            file.write(f"{word}, {freq[word]}\n")
    print(f"結果を保存しました: {new_file_path}")
if __name__ == '__main__':
    root = tk.Tk()
    root.withdraw()  # Tkのルートウィンドウを表示しない
    file_path = filedialog.askopenfilename( title='ファイルを選択してください', filetypes=[('テキストファイル', '*.txt'), ('すべてのファイル', '*.*')] )
    if file_path:
        freq = get_frequency(file_path)
        save_frequency_list(file_path, freq)
    else:
        print('ファイルが選択されませんでした。')
```
